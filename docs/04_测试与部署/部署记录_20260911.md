# 2026-09-11 演示版发布

用户后续确认部署完成。确认对象为此前推送的 `259a633`；本地第五批整改尚未再次推送。自动线上冒烟仍因原预览链接401未完成，用户确认与工具实测分开记录。

目标：独立项目 path-edu-platform，GitHub main → 现有 EdgeOne 项目。旧科研平台不变。

发布前基线：51项自动测试和生产构建通过；师生11模块在1440／1280／768／390px无整页横向溢出；题库预览键盘焦点、取消导航、刷新草稿恢复通过。详细边界见设计审计清单。

本次发布为无数据库演示。默认 `/` 使用公开演示身份和本浏览器数据；不配置真实数据库，不上传本地数据、凭据或环境文件。`/account` 仅保留待配置真实账号服务，不能作为本次演示验收入口。

状态：已推送 `259a633923916d30f3bc044742c99e957bd9a15d` 至 origin/main；GitHub未返回构建状态。访问原 EdgeOne 地址返回401（Access Restricted or Authentication Expired），尚未确认云端构建及线上验证，需要控制台新预览链接和该提交的部署状态。此前远端版本为 `586f42154de987caab984f10329e53cd369447fc`，可作为旧版回退参照。优先使用云端重新部署旧提交，不强制回退Git分支，不清除用户浏览器记录。

未完成：存储故障注入、20+分类、200%缩放及审计剩余布局项；AI、语音转写、跨设备共享数据和WSI均不在本次发布内。

## 后续发布（同日，视觉整改与考试重构）

同日晚间按用户反馈连续推送四次，均已到达 origin/main：

| 提交 | 内容 | 推送前门禁 |
|---|---|---|
| `87eb051` | 页头／筛选纵向居中与撑满；返回统一为文字链接；去掉 eyebrow 与全局演示横幅 | 11/11 脚本、57 项测试、typecheck、build |
| `35cf06f` | 列表首列单行＋省略号＋hover 全文；补 favicon；四维能力画像条形图 | 同上 |
| `de04260` | 考试与阅片重构：学生导航收窄；考试中心卡片网格；考试全屏模式；成绩结果页重做；图书馆全屏＋阅片助手 | 同上 |
| `fed294e` | SlideSeek 导航阅片 | 同上 |

**部署方式**：GitHub main 推送触发 EdgeOne 自动构建，与 `259a633` 同一条链路。
**工具侧无线上冒烟证据**：本环境取不到 EdgeOne 控制台，也没有可用的预览链接；
「是否构建成功、线上是否为新版本」属**用户确认**事项，须与控制台核对后单独记录，不得写成工具实测。
