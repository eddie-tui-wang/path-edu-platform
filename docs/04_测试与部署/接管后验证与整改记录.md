# 接管后验证与整改记录

日期：2026-09-11。当前参照提交：f9fc5c3。按步骤追加实测证据，不以历史结果替代本次验证。未授权推送或部署。

## 1. 浏览器存储故障验证（K4）

- 问题：浏览器写入失败时的数据保护缺少实测证据。
- 文件：`scripts/verify-storage-failure-browser.mjs`；业务代码暂未修改。
- 方式：独立临时 Chrome 上下文，针对练习草稿、题库指定存储键模拟 QuotaExceededError；恢复写入后重试。
- 风险控制：不连接用户 Chrome 资料，不清除既有记录，不改服务端数据。
- 实测：脚本退出码 0。练习错误提示、无虚假保存成功、返回／跨模块取消离开、已选答案保留、重试、重新打开恢复通过；题库错误提示、题干保留、继续编辑、重试、保存后禁用按钮通过。
- 环境记录：普通沙箱启动 Chrome 因权限失败；经授权运行后可正常测试。这不是 Playwright 缺失。首次脚本与 Next 隐藏 alert 匹配冲突，已收窄到具体错误信息后重跑通过。
- 边界：本脚本练习为单选；阅卷补测见下节。练习简答、考试草稿与 sessionStorage 清理失败尚未覆盖，不称所有存储路径已验证。独立浏览器测试不增加单元测试计数。

### 阅卷补测与修复

`verify-publishing-browser.mjs` 在真实演示组卷／交卷之后，对评分草稿写入故障注入。实测复现：一次成功保存后，下次失败仍显示旧“评分草稿已保存到本机”。仅修改 `app/grading-workspace.tsx` 的 cache 异常分支清空 notice。复跑通过：错误不再与旧成功提示并存；取消离开保留点评；恢复写入可重试、复核、发布。没有改变评分计算或历史数据结构。

## 2. 过期脚本重写（K11）

已先输出当前组卷、开考、交卷、阅卷和管理员页面的可访问快照，再更新脚本；以下均实测退出码0：

| 脚本 | 本轮验证范围 |
|---|---|
| verify-navigation-browser | 未保存题干取消跨模块离开；保存后跨模块恢复；学生图书馆详情刷新和返回 |
| verify-demo-browser | 三个角色登录；管理员弹窗新增账号；刷新重登；停用拒绝登录；不调用真实账号API |
| verify-publishing-browser | 十片默认简答卷发放；指定学生作答1题并交卷；发布前不显示成绩；教师故障重试、复核与发布；学生看到8分、点评及10题结果 |

范围更正：发布脚本现验证题库组卷，不再承担旧脚本的图片导入；导航不再断言已经移除的旧字段定位按钮。图片导入、混合卷浏览器全链路仍不在本次覆盖范围；不能从脚本通过推断其已验证。

## 3. S05 与文档一致性

S05 已实施：仅练习有提交结果时增加辅助区，≥1400px四列，低于1400px使用独立标签及关闭动作；AI输入和发送仍禁用。使用练习组件内部临时状态，不扩展共用标签白名单；刷新回到保存的合法作答标签，避免影响考试。

`verify-practice-assist-browser.mjs` 四档宽度、提交前隐藏、AI禁用、窄屏开关、刷新回答案已通过。200%独立复验结果以项目状态补充为准。

K8 保留记录，不删除样式；归档快照不改。文档一致性修订仅改变当前事实描述，不回写历史验收。

## 4. 基线与交付边界

本轮 `npm test` 52项通过、`npm run typecheck` 通过、`npm run build` 通过。它们调用原 package.json 脚本，没有安装新依赖或改锁文件；pnpm typecheck 在当前终端未返回输出，已终止，未记为通过。

全部变更留在本地，未提交、推送、部署。原 next-env.d.ts 自动变动不作为手工修改。测试使用独立浏览器，不改用户现有题库、答卷或成绩。

## 5. 独立复验（第二次，2026-09-11）

在上一批实现之上重新实测，**不复用历史结果**：

- `pnpm test` **52 项通过、0 失败**；`pnpm typecheck` **exit 0**（补上 §4 里「pnpm typecheck 未返回输出，不记为通过」的缺口）；`pnpm build` 通过。
- 浏览器脚本 **9 个、共 11 次运行，全部 PASS**：
  - 本批新增／重写：`verify-storage-failure-browser`、`verify-practice-assist-browser`、`verify-navigation-browser`、`verify-demo-browser`、`verify-publishing-browser`
  - 回归：`verify-detail-browser`（100% 与 200%）、`verify-annotation-tail-browser`（100% 与 200%）、`verify-layout-browser`、`verify-audit-editor-browser`
- 文档链接：41 份活动文档、172 个相对链接、**0 失效**。

**结论**：第五批列出的 5 项剩余重点（S05／20+分类／故障注入／200%缩放／详情全流程）**全部关闭**；K11 的 3 个过期脚本**全部关闭**。

**本轮未改变任何业务代码**，只做文档一致性修订与提交。§1 的未覆盖边界未被本轮改变，仍然有效：练习简答、考试草稿、`sessionStorage` 清理失败；图片导入、混合卷浏览器全链路。

**一处更正**：`项目状态.md` 顶部补充曾写「阅卷等未覆盖，K4 仍开放」，与本文 §1「阅卷补测与修复」冲突——阅卷写入故障实由 `verify-publishing-browser.mjs` 覆盖并通过，已在 `项目状态.md` 改正。

## 6. 收窄验证缺口（2026-09-11 第三轮）

按 §1 与 §5 列出的未覆盖边界逐项补齐。**未改变任何业务代码**，只扩展与新增验证脚本。

### 6.1 存储故障注入：三个缺口已补齐

| 覆盖 | 脚本 | 实测内容 |
|---|---|---|
| 练习**简答** | `verify-storage-failure-browser.mjs` | 与单选共用草稿键但编辑器不同：写入失败后显示错误、无「已保存到本机」、文本保留、取消离开仍保留、恢复后可重试保存 |
| **考试草稿** | `verify-publishing-browser.mjs` | 交卷前注入答卷写入失败：出现「重试保存」、显示错误、原答保留、无误报成功；恢复后重试保存并继续交卷 |
| **sessionStorage 清理失败** | `verify-storage-failure-browser.mjs` | 题库草稿「放弃修改」时清理失败：**编辑器不关闭、不跳转、错误显示在弹窗内、已填内容保留**；恢复后可正常退出 |

**修复脚本自身的一个缺陷**：故障注入助手原先只在首次调用时安装补丁，`restore()` 还原后再次注入会**静默失效**（前缀已设置但补丁不在）。已改为每次注入都重新安装补丁。注意 `restore()` 只还原、不卸载前缀，所以再次注入必须重装。

**仍未覆盖**：练习「返回练习列表」时的 `sessionStorage` 清理失败（仅提示错误、仍会返回列表，数据保护语义弱于题库路径）；练习提交失败与病例保存失败的浏览器侧注入。

### 6.2 图片导入（新增脚本）

`verify-case-image-browser.mjs`：超过 500 KB 的文件被拒且**不产生任何副作用**；合法 PNG 可导入；超过 6 张被拒；可移除草稿图片；保存后回到图书馆列表并能检索到该病例，存量数据确认 5 张图片、使用确认为 true、**未勾选开放给学生即不发布**。

> 注意：新建病例的「确认有权使用」复选框默认**未勾选**，原生校验会阻止提交。这是有意的显式确认，不是缺陷——但它会让只填文字的自动化脚本静默卡在表单上。

### 6.3 混合卷浏览器全链路（并入发布脚本）

`verify-publishing-browser.mjs` 新增第二遍：单选 + 十道简答的**混合卷**（11 题 / 10 片 / 110 分）——发放、学生开考、确认单选渲染选项组而简答渲染文本区、作答并交卷、教师复核（简答需人工给分）、整场发布、学生看到成绩与全部 11 题。

> 同时发现一个**预期行为**（非缺陷）：考试工作区会**恢复上一次答卷**，同一浏览器再次进入考试中心时先停在上一场的结果页。脚本需先「返回考试列表」才能选另一份卷。

### 6.4 本轮基线

`pnpm test` 52 项、`pnpm typecheck`、`pnpm build` 通过；**10 个浏览器脚本、12 次运行（含 2 次 `NATIVE_ZOOM=200`）全部退出码 0**；活动文档 175 个相对链接 **0 失效**。

变更仍留在本地，**未提交、未推送、未部署**。

## 7. 示例卷（2026-09-11 第四轮）

需求：演示时能直接给老师看现成的卷子。**未改变任何既有业务逻辑**。

### 7.1 种子题库补齐单选

十片是硬校验（`bank-paper.mjs:18` 要求每卷 10 个不同影像记录），而种子题库原本只有 1 道单选，因此**全单选十片卷在数据上不可能存在**。已补 9 道单选 `DEMO-SINGLE-002..010`，每病例一道，与既有的 `DEMO-PRACTICE-001` 合成 10 道，覆盖病例 01–10。种子题库现为 **10 单选 + 10 简答**。

题干只考察读记录（字段是否缺失、素材来源、能否据此下结论），**不含医学结论**。

### 7.2 三份示例卷

`lib/sample-papers.mjs` 生成，**复用 `buildBankPaper`**：与教师手工组卷走完全相同的十片与卷型校验。混合卷取 5 单选（病例 01–05）+ 5 简答（病例 06–10），病例互不重叠以满足十片。

调用点：`BankPaperWorkspace.refresh()` 与 `ExamWorkspace.refresh()`。固定 ID 幂等，不覆盖教师自建卷。

### 7.3 连带修正（都是本次改动暴露出来的脆弱假设）

| 文件 | 问题 | 处理 |
|---|---|---|
| `tests/bank-paper.test.mjs` | 用**位置索引**取种子题（`bank[1]` 原为第一道简答，补单选后变成了单选） | 改为按题目身份取（`shorts[0]`），不再依赖种子顺序 |
| `tests/practice.test.mjs` | 断言种子总数为 11；`rows[1]` 取简答题 | 改为 20 并分别断言 10 单选／10 简答；简答题改为按类型查找 |
| `verify-storage-failure-browser.mjs` | 点「开放练习」的 `.first()` 原本命中简答，补单选后命中单选，导致取不到简答练习 | 限定到含「简答」的题目卡片 |
| `verify-publishing-browser.mjs` | 「阅卷与成绩」在试卷增多后出现 4 个匹配（严格模式冲突）；「开始考试」同理 | 均限定到目标试卷卡片 |

### 7.4 基线

`pnpm test` **55 项**（新增 3 项示例卷测试）、`pnpm typecheck`、`pnpm build` 通过；**10 个浏览器脚本、12 次运行全部退出码 0**。浏览器实证：教师「考试管理」显示 3 份示例卷且均为「开放中」「10题 · 100分」，学生「考试中心」3 份均「可开始」。

变更仍留在本地，**未提交、未推送、未部署**。

## 8. 切片浏览重构：移植旧阅片界面（2026-09-11 第五轮）

按 D-50 执行：把退役的 `app/teaching-module.tsx` 阅片界面移植到切片图书馆详情，**配色改用当前 design system**，标注不持久化。

### 8.1 关于「旧页面能不能直接复用」的结论

组件与 CSS 都还在（`.teaching-*` 系列在 `migrated.css` 19 处、`globals.css` 18 处），**但不能直接挂载**：

| 阻塞点 | 证据 |
|---|---|
| 硬编码单一病例 | `if(caseId!=="gastric-001")` → 其余 12 个病例进不去 |
| 语境不符 | 它是「考试作答」工作区（intro／analyzing／result 三阶段 + 六段答题表） |
| 深色工作台 | `.teaching-viewer{ background: var(--navy-deep) }`，与当前浅色体系冲突 |
| 尺寸契约 | `min-width:1180px`，在窄屏会产生整页横向溢出 |

因此**移植骨架与交互逻辑，不 import 组件**：新建 `app/case-viewer.tsx`，复用其布局结构、工具盘、`addMark` 逻辑与标注样式。

### 8.2 移植内容

- **工具盘**：平移／缩放／矩形／圆形／点标注／自由笔，与旧版一致；`缩放` 与 `自由笔` 沿用旧版的**禁用**状态（旧版即未实现）
- **病例信息栏**：`<details>` 手风琴，保留当前全部字段（主诉／现病史／既往史／检查结果／送检信息／资料来源）
- **切片舞台**：缩放平移、方向键平移、加减键缩放、缩略图切换、`role="region"` 可聚焦
- **标注**：`addMark` 逐字移植（`x/y` 百分比 + 5–94／6–92 夹取 + 记录当时倍率），三种形状沿用旧版分类色
- **改动**：chrome 由 `--navy-deep` 改为 `--surface`／`--background`／`--primary-soft`；标注分类色**保持不变**（AGENTS.md 禁止为配色重染标注语义色）

### 8.3 有意不移植

旧版的 intro／analyzing／result 三阶段与六段答题表——那是考试作答流程，不属于图书馆浏览。

### 8.4 同批：图书馆标题行

「全部切片／我的收藏」原在标题下方独立一行，已并入标题行右侧。实测 teacher／student 标题与切换按钮同在 `top≈193`，无整页横向溢出。

### 8.5 验证

新增 `scripts/verify-case-viewer-browser.mjs`：工具盘与两个禁用工具、三种标注形状各自成图、标注栏默认折叠再展开、**点击已有标注是选中而非叠加**、方向键平移 +24px、缩放与复位、舞台面积主导信息栏、桌面宽度无整页横向溢出、学生端同样可用但无教师编辑入口。

可测量证据（1440px）：舞台／信息栏面积比 **2.08**；文字对比度 标题 **12.12:1**、折叠项 **11.31:1**、辅助文字 **5.28:1**，均高于 4.5:1。

> **诚实边界**：当前模型无法读图，**未做视觉目检**；以上是几何与对比度的程序化测量，不等于视觉验收。

## 9. 作答界面重构：固定舞台 + 可收合轨道（2026-09-11 第六轮，P2）

按已确认的设计方向执行：练习与考试作答改为「固定的切片舞台 + 两侧可收合轨道」，AI 追问按 copilot 形式由按钮拉出／隐藏。

### 9.1 布局与实测比例（1440px）

三栏网格 `minmax(220px,16rem) | minmax(0,1fr) | minmax(300px,21rem)`，实测：

| 状态 | 左轨 | **舞台** | 右轨 | 舞台／右轨 | 舞台占比 |
|---|---|---|---|---|---|
| 展开 | 256 | **560** | 336 | **1.67（≈φ）** | 49% |
| 收起左轨 | 0 | **832** | 336 | — | 71% |

考试端同为 256／560／336。**切片始终是最宽的一栏，且约为作答栏的 1.67 倍**——这是本轮的验收指标，已写进脚本断言（`stage/answer > 1.5`）。

### 9.2 AI 追问改为 copilot 轨道

- 入口是**舞台工具栏右侧的按钮**，提交前不存在（无可追问的对象）
- 默认**收起**；展开后不改变舞台宽度（脚本断言 `stageBefore === stageAfter`）
- 面板是 `<aside aria-label="AI追问辅助">`，语义为补充内容
- 输入框与发送**保持禁用**——服务未接入，不制造可用假象（沿用既有硬规则）
- **为何不做第四个独立列**：1440 下工作区内容宽 1184，左 256 + 舞台 300 + 作答 336 + copilot 280 = 1172，仅剩 12px 余量，会把舞台压到 300px 以下，直接违背「舞台主导」。故 copilot 落在右轨内、作答之下。

### 9.3 顺带清理（D-49 的直接后果）

窄屏适配既已退出范围，原 `.practice-tabs` 标签机制（把三栏在小屏折叠为标签）成为死代码，已移除：

- 删除 CSS：`.practice-tabs`／`.practice-show-*`／`.practice-columns`／S05 的 `.practice-result` 与 `≤1399px` 分支、`≤1100px` 分支
- 练习与考试不再使用 `useAnswerPart`；`app/use-answer-part.ts` **暂不删除，仅标注为死代码**（按 AGENTS.md「只记录不删除」的既有做法，与 K8 同类）

### 9.4 验证

- `verify-practice-assist-browser.mjs` 重写：提交前无入口、舞台最宽且 >1.5 倍、收起左轨后舞台变宽而位置不变、copilot 默认收起、展开不改变舞台宽度、AI 输入与发送禁用、再次收起后 `aria-pressed=false`
- `verify-detail-browser.mjs` 更新：断言三栏存在、舞台最宽、左轨收起归零并可恢复

变更仍留在本地，**未提交、未推送、未部署**。

## 10. 全局规范落地与列表密度（2026-09-11 第七轮，P3）

### 10.1 两条全局规范（用户 2026-09-11）

**返回按钮一律左上。** 落地为 D-52：页头第一个元素、统一 `← 返回X`。共迁移 12 处——此前散落在 8 个文件的页头右侧或页脚。

一个技术细节值得记录：箭头最初用 CSS `::before` 生成，实测发现 **Chrome 会把生成内容计入可访问名**，导致 `返回图书馆` 匹配失败。改为 `<span aria-hidden="true">←</span>` 后，可访问名保持 `返回X`——箭头是纯视觉提示，本就不该进入可访问名。

**范围切换与标题同行。** 落地为 D-53 并写入 `交互与UI原则.md` §3 页头结构。同时澄清一处易混边界：`卡片／列表` 是**布局**切换（不改变数据集合），仍留在结果工具区（DS-03）；一并挪到标题行会违反既有分区原则。

### 10.2 列表密度

新增 `.data-table`：一行一条记录、带表头行、列宽由各表用内联 `--cols` 声明，因此不锁定列数（既有 `.teacher-record-table` 固定 6 列，无法直接复用）。视觉语言与之一致。

| 列表 | 改造 | 实测 |
|---|---|---|
| 学生 学习记录 | 卡片 → 表格 | 1 行 / 69px，表头「题目／考试 来源 时间 得分 操作」 |
| 学生 错题集 | 卡片 → 表格 | 1 行 / 62px，表头「题目 来源 部位 版本 操作」 |
| 教师 题库 | 卡片 → 表格 | **20 行 / 63px**，表头「题干 题型 病例 版本 状态 操作」 |
| 教师 考试管理 | 卡片 → **行内 accordion** | 3 行 / 93px；展开行内显示试卷内容与逐题预览，取代列表下方的独立预览区 |
| 教师 教学记录 | 已是表格 | 未改动，作为范式来源 |

考试管理的改动顺带修掉一个 Proximity 问题：预览原先渲染在列表**下方**，远离触发它的行；现在行内展开。

### 10.3 一次自查发现的回归

把考试列表预览改为行内 accordion 时，我一度**整段删除了原预览区**——但该区块同时服务编辑页第 3 步「检查并预览」，内含 `确认发放给所选学生`，会使**发放流程失效**。已恢复为仅编辑态渲染（`preview && draft`），`verify-publishing-browser` 覆盖该路径并通过。

### 10.4 验证

`pnpm test` 55 项、`pnpm typecheck`、`pnpm build` 通过；**11 个浏览器脚本全部退出码 0**。四张新表格均无整页横向溢出。

变更仍留在本地，**未提交、未推送、未部署**。

变更仍留在本地，**未提交、未推送、未部署**。
