# 师生端交互与UI审计

## 第六批整改（2026-09-11，接管回合之后）

| 项目 | 本批实施 | 验证边界 |
|---|---|---|
| S05 | 练习结果新增 AI 追问辅助区：仅在有提交结果时出现；≥1400px 为第四列（右侧辅助），低于 1400px 走独立标签并提供「关闭追问」；AI 输入与发送仍禁用 | 四档宽度、提交前隐藏、AI 禁用、窄屏开关、刷新回答案标签均已实测通过。使用练习组件内部状态，未扩展共用标签白名单，不影响考试页 |
| K4 | 新增 `verify-storage-failure-browser.mjs`：对练习草稿与题库指定存储键注入写入故障 | 练习单选、题库、阅卷三处写入失败已实测通过。**练习简答、考试草稿、sessionStorage 清理失败未覆盖** |
| K11 | 重写 3 个过期脚本：`verify-navigation-browser`／`verify-demo-browser`／`verify-publishing-browser` | 均实测退出码 0。发布脚本现验证题库组卷，**不再承担旧脚本的图片导入断言**；图片导入与混合卷浏览器全链路不在覆盖范围 |

同时修复：阅卷评分草稿保存失败时，旧的「评分草稿已保存到本机」提示会与错误并存。仅改 `app/grading-workspace.tsx` 的 cache 异常分支清空 notice，未改评分计算与历史数据结构。

**2026-09-11 独立复验**（接管回合第二次，与上一批实现分开记录）：`pnpm test` 52 项、`pnpm typecheck`、`pnpm build` 通过；浏览器脚本 9 个、共 11 次运行（含 2 次 `NATIVE_ZOOM=200`）**全部 PASS**。

至此，**第五批列出的 5 项剩余重点（S05／20+分类／故障注入／200%缩放／详情全流程）全部关闭**。

仍未关闭：T07「完整发布后返回实页」、S10「长卷人工阅读」；存储故障注入的练习简答／考试草稿／sessionStorage 分支；图片导入与混合卷浏览器全链路；云端部署状态未确认。
## 第五批整改（2026-09-11，已部署版之后）

| 项目 | 本批实施 | 验证边界 |
|---|---|---|
| T07 | 考试列表展示实际已提交、已复核、待复核、成绩已发布份数；从阅卷返回重新读取 | 单元测试覆盖多次复核不重复计数、排除未提交和其他考试；完整发布后返回实页待复验 |
| S08 | 搜索在前，来源与日期紧随，沿用清空和空态 | 四档宽度无整页溢出 |
| S10 | 明确题目版本／试卷版本／成绩发布版本；多题记录按题号折叠，单题默认展开；病例图片二级展开 | 保持只展示已发布参考；历史无题库版本时不伪造版本。版本及未发布隔离由自动测试覆盖；长卷人工阅读实测待补 |
| C04 | 移除教师页重复全宽长提示，保留统一环境提醒、结果示例来源与无真实音频说明 | 回归测试改为检查必要来源仍在、重复提示不存在，不再绑定已删除的长句 |

构建通过；师生11模块在1440／1280／768／390px无整页横向溢出。新增进度计算回归，自动测试总数52项。此批为本地改动，未自动再次部署。剩余重点：S05 AI追问区域、20+分类、故障注入、200%缩放及详情全流程验证。**（该批 5 项剩余重点已于 2026-09-11 全部关闭，见上方第六批。）**

**2026-09-11 接管回合补记（已浏览器复验）**：`pnpm test` 52项、`pnpm typecheck`、`pnpm build` 通过。随后用 Playwright 1.62.1 + 系统 Chrome 实跑浏览器脚本：

- `verify-annotation-tail-browser.mjs` **PASS**：31 个部位的搜索弹层（滚动、空态「没有匹配部位」、键盘 Enter 选定、Escape 关闭并回焦触发器）、清空筛选、草稿禁用保存、确认入库后定位并清空筛选、教师录音单一归属、无重复卡／返回／横幅、无整页横向溢出。
- 同脚本 `NATIVE_ZOOM=200` **PASS** —— 上表 S08／S10／C04 中「200% 缩放」这一项已补齐证据。
- `verify-layout-browser.mjs` **PASS**：11 模块 × 1440／1280／768／390px 无整页横向溢出（由自动几何检查升级为真实浏览器实测）。
- `verify-audit-editor-browser.mjs` **PASS**：筛选键盘／焦点、预览焦点陷阱与 Escape、跨模块取消离开、编辑页四档宽度、刷新草稿恢复。

**20+ 分类压力测试已实测通过**（31 个分类），不再是「未伪造 20 类实测」。

**详情全流程已补齐**：`verify-detail-browser.mjs` 原先指向 2026-09-09 改造前的旧界面而失效，已于 2026-09-11 按现界面**重写并通过**——覆盖师生 11 模块、图书馆／能力／教学记录／练习四条详情链路、hash 路由刷新恢复、浏览器前进后退、窄屏面板切换与图片缩放控件；原生 200% 缩放下同样通过。

上表仍未关闭的是：T07「完整发布后返回实页」、S10「长卷人工阅读」，以及**存储故障注入（浏览器侧）**。此外 `verify-navigation-browser`／`verify-demo-browser`／`verify-publishing-browser` 仍是改造前的旧界面选择器（见 [交接说明](../00_总览/交接说明.md) K11）。

本回合同时修正 S10 的一处重复显示：练习记录的记录级版本就是题目版本，而详情原先把同一版本显示两遍（记录标题「题目版本 N」与逐题「题目版本 N」各一次），与 DS-07「状态只出现一次」冲突。现改为练习记录逐题不再重复显示版本，并新增断言锁定该不变量；考试记录仍保留「成绩发布版本」与逐题「题目版本」两级，因为两者含义不同。

## 第四批整改与交互复验（2026-09-11）

| 项目 | 改动／实际验证 | 边界与未完成项 |
|---|---|---|
| T03 | 题库未保存修改新增侧栏、退出、改密及刷新离开保护；取消离开仍保留题干；刷新重新登录可恢复暂存。清除暂存失败不关闭编辑器、不覆盖错误为成功提示 | 跨模块沿用原生确认；确认入库后定位条目仍待补；浏览器存储写入／清除失败尚未故障注入 |
| T04 | 排序使用带可访问名称的轻量箭头工具，删除同组；窄屏将工具放到下一行。题干补稳定可访问名称 | 编辑页1440／1280／768／390px未整页横溢；长选项和触屏操作仍需单独验证 |
| T01/S01 | 搜索甲状腺，方向键与Enter选定，关闭后焦点回触发器；Escape关闭验证通过 | 本次使用现有分类，20+分类压力测试未执行 |
| T03/C03 | 预览原生dialog的Tab不能进入背景控件，Escape关闭并回焦原预览按钮 | 原生浏览器允许Tab转向浏览器界面；200%原生缩放和软键盘未测试 |
| C02及列表断点 | 教师5模块、学生6模块在1440／1280／768／390px共44个列表视口检查，无整页横向溢出 | 此为自动几何检查，不代表所有表格操作、详情和视觉细节均通过 |

可复跑证据：`scripts/verify-layout-browser.mjs`、`scripts/verify-audit-editor-browser.mjs`。使用独立临时Chrome上下文，未读取用户浏览器资料，未改变现有题库、答卷或成绩。51项现有自动测试及构建通过；新增浏览器检查为独立脚本，不计入51项。

仍需继续：存储故障注入、20+部位、200%缩放、移动输入法，以及T07列表进度、S05追问布局、S08筛选顺序、S10长答卷、C04重复提示。未宣称全量审计或上线验收完成；未部署。

**2026-09-11 状态核对**：上述除「**移动输入法仍未验证**」外——20+部位、200%缩放、T07列表进度、S05追问布局、S08筛选顺序、C04重复提示**均已关闭**；存储故障注入**部分关闭**（练习单选／题库／阅卷已验，练习简答／考试草稿／sessionStorage 未验）；S10 的「长答卷人工阅读」与 T07 的「完整发布后返回实页」**仍未做**。

## 第三批整改（2026-09-11）

| 项目 | 实施与证据 | 尚待验证 |
|---|---|---|
| S03 | 练习／考试共用纯文本工具，支持选区加粗标记、多行列表、撤销重做和长度校验；已交卷只读 | 组合输入法、触屏选区及全部快捷键实测 |
| S04 | 按账号／题目版本或答卷题号保存图片、缩放、平移和内容标签；练习位置支持刷新恢复；仅改视图记录，不改答卷结构 | 实页独立验收学生将图片调至125%，返回练习再进入，仍为图片标签和125%；考试逐题、刷新及多图回归待补 |
| S06 | 独立考试状态标签，已发布显示“查看成绩”；历史配置缺失单独显示 | 实页已见“成绩已发布”及对应按钮；其他时间状态由单元测试覆盖 |
| S07 | 原生模态dialog管理焦点；显式确认参数避免旧确认绕过开考；返回／已提交后关闭确认 | 浏览器Escape、焦点回位、超时与弹窗竞态需专门验证 |

自动测试51项通过，构建与类型检查通过。本批浏览器只查看已发布成绩和调整独立验收学生的阅片位置；未开始新考试、未提交练习、未改成绩。全量设计门禁仍未关闭，不能以这些局部通过替代窄屏、键盘和存储失败复验。

## 整改进度（2026-09-10，第一批）

### 第二批：组卷、阅卷与保存保护

| 项目 | 已实施 | 验证与剩余事项 |
|---|---|---|
| T06 | 新建与继续草稿拆开；新建替换旧草稿、默认十题替换已有选题须确认；选题／配置／检查编号；未保存离开提示 | 构建通过；替换确认目前沿用原生确认，尚需统一页内弹窗与完整交互回归 |
| T07 | 试卷名称搜索、开放状态筛选、清空、中文本地日期 | 教师实页确认2份卷、开放中状态及本地时间；提交／复核进度在阅卷页显示，列表汇总待补 |
| T08 | 姓名为主、账号为辅；答卷题号定位及待评标识；顶部复核保存入口 | 实页读取独立验收学生既有答卷，姓名、10题定位、保存状态可见；未修改评分 |
| T09 | 评分输入自动存本机草稿；重新打开恢复；正式评分版本变化时不混用旧草稿；草稿不产生复核／发布；保存失败重试及离开保护；发布前数量 | 新增回归覆盖草稿恢复、权限、存储失败、过期评分冲突与不泄露成绩；实页确认1已提交／1已复核／0待复核。浏览器刷新及故障注入仍待补 |
| S02 | 练习存储失败时清除成功提示；重试、文本选中复制、返回／侧栏／关闭保护；撤销与普通输入共用保存行为 | 构建通过；存储耗尽等浏览器故障注入未执行，不标全面通过 |

第二批48项自动测试通过，生产构建及类型检查通过。仅切换演示身份并读取试卷／答卷，没有新增考试、改分、重新发布、清除历史数据或部署。剩余重点：S03/S04文本工具与阅片连续性、S06/S07考试状态及确认焦点、全量键盘与窄屏复验。

以下状态覆盖本文历史“全部待整改”的描述；未列项仍待实施，不代表全量验收通过。

| 项目 | 已实施 | 验证与剩余事项 |
|---|---|---|
| T02 | 草稿与可用版本分离；练习查询、提交、组卷均引用最近非草稿版本；停用不会复活更早版本；题库显示当前可用版本 | 内存回归验证旧练习可继续提交、旧题可组卷、停用与确认新版本规则；不改用户历史数据 |
| T10/T11 | 教学记录页头统一两个动作；能力页移除录音；根页去返回；详情去四维摘要及重复完成卡 | 服务端渲染回归通过；完整浏览器多入口复核待补 |
| T12 | 移除固定覆盖率、虚构证据数量、示意雷达；保留四维证据入口与短示例标签 | 已构建；量化评价仍未接入，不宣称医学评价有效 |
| T01/S01/C01 | 共享可搜索部位下拉，集中搜索／部位／来源；视图切换独立；统一原生select箭头和焦点 | 学生实页搜索甲状腺并选定，结果3条；清空入口已观察。20+、全键盘与规定断点仍待实测 |
| T03/T05 | 去结束编辑；顶部只读预览弹窗；返回时保存／放弃／继续；确认后返回列表；无改动草稿禁用保存；AI未接入按钮禁用并说明 | 构建通过；编辑回归、定位已入库条目与跨模块离开保护仍待补，不标完全关闭 |
| S08/S09 | 学习记录区别筛选空态、清空筛选、日期倒置提示、时间倒序；五维未评估压缩为一组说明 | 实页看到“没有匹配的学习记录”与清空按钮并执行清空；其他组合待补 |
| C03 | 小屏取消题库固定底栏，预览独立原生dialog | 构建通过；软键盘及200%缩放未测 |

本批自动测试47项通过，生产构建及类型检查通过；未部署、未推送、未重置演示记录。后续继续状态保护、组卷／阅卷效率、图片视野连续性及全量交互复验。

日期：2026-09-10。基线：[交互与UI原则](./交互与UI原则.md)。结论：功能链路可运行不等于设计验收通过；当前存在信息层级、任务归属和状态表达问题，应先改结构再统一控件。

## 范围与证据

### 正式复核补充（用户确认audit后）

本轮在本地实际页面复核教师图书馆、教学记录、能力总览，以及独立验收学生的能力分析和学习记录；没有创建或修改病例、题目、试卷、评分。草稿问题使用进程内Map存储复现，未写浏览器数据。沿用既有问题编号，不另建重复清单。

| 证据编号 | 操作／读取 | 实际结果 | 关联问题与状态 |
|---|---|---|---|
| E01 | 教师图书馆当前页面 | 七个部位以卡片独占区域；来源、搜索、视图和清空在另一组；选中泌尿类别显示2条 | T01：实页确认；20+类别为规模风险，未伪造20类实测 |
| E02 | 教学记录→能力总览 | 两页均首先出现录音分析标题与按钮、返回病例资料；教学记录创建按钮仍处筛选区 | T10：实页确认，违反任务归属及根页面导航规则 |
| E03 | 教师能力总览 | 3条有效记录、76%覆盖率、18/12/9/2证据与6/3条入口均存在 | T12：显示已确认；统计依据不能由界面证实，仍为固定示例，不认定真实量化 |
| E04 | 内存中seedQuestions→saveQuestion(confirm=false)→availablePractice | 保存前可用练习1条，保存后0条；新版本revision=2、status=draft、practiceOpen=false | T02：行为已复现，最先修复；没有操作用户题库 |
| E05 | 独立验收学生能力页 | 1场已发布考试、10题；五个未评估卡在证据列表之前 | S09：实页确认，数据真伪边界正确但信息密度不合理 |
| E06 | 学习记录搜索不存在名称，然后清空 | 已有1条记录；搜索后却显示“暂无学习记录”，没有清空入口；手动清空后恢复 | S08：行为已复现，空态应改为“没有匹配记录”并提供清空筛选 |

审计结论：不通过当前Design System设计门禁。已有27条检查项（12教师、10学生、5共性）是检查位置数，不是27个独立根因；共享图书馆等应合并整改。最优先为T02草稿影响可用题、S02保存失败离开保护（源码依据、未做浏览器故障注入）、T10/T11教师层级及T01共享筛选。

本轮没有重跑390px、200%缩放、屏幕阅读器和全部控件六态；这些仍是待验证，不以桌面读取结果替代。既有功能测试通过不能抵消本次设计问题。两项设计技能用于信息层级与分组判断，Ponytail用于复用及简化方向，不将审计擅自变为代码修改。

本轮覆盖教师图书馆、出题、组卷、阅卷、教学记录、分析详情、能力总览；学生图书馆、日常练习、考试、结果、学习记录、错题、能力分析。依据为用户9张截图、当前对应组件与共享样式源码，并参考前轮实际浏览器师生流程。没有在本轮逐一重跑所有交互，也没有测得390px、屏幕阅读器或所有对比度数据；相关项标为待实测而非通过。

优先级：P1影响理解、工作流或数据预期；P2影响效率、一致性或可发现性。以下均为整改建议／验收要求，本轮没有修改业务页面。

## 九条批注的处理结论

| 批注 | 判断 | 确定的设计方向 | 原则 |
|---|---|---|---|
| 1 四维卡意义不明 | 原意是摘要，但没有本次对象与证据，且出现在记录标题前，将长期画像和单次评价混淆 | 从单次详情移除重复四维卡；本次反馈只跟随实际证据，长期四维留能力总览 | DS-01/06 |
| 2 部位卡铺开 | 分类实际是筛选，超过20项将压低结果可见性 | 与搜索、来源合并；“部位：全部 ▾”可搜索下拉，四类优先，旧类保留，单选语义不变 | DS-03/04 |
| 3 四个编辑按钮 | 草稿、预览、确认各有独立用途；“结束编辑”清暂存却像导航 | 保留前三项并分层；预览移工具区，返回负责退出及未保存处理，删除独立“结束编辑” | DS-05 |
| 4 返回病例资料 | 一级模块不是从病例详情钻入，返回无上下文 | 教学记录／能力总览根页移除；记录详情保留返回来源 | DS-08 |
| 5 重复免责声明 | 多处同义句挤占任务区域 | 删除重复段落，保留一个短示例标识；错误、风险及真实服务状态仍如实表达 | DS-09 |
| 6 开展教学混在筛选栏 | 创建动作与筛选关系不同 | 教学记录页标题右侧放“开展教学阅片”和“录音分析”，搜索栏只做搜索筛选 | DS-03 |
| 7 录音出现两处 | 同一业务重复归属，能力页首屏被操作占用 | 唯一入口在教学记录，能力总览不放录音操作块 | DS-02 |
| 8 三个已完成卡 | 原意是后台任务状态，但与标签同名且固定成功，信息重复 | 移除卡组，总状态放记录标题旁；异常在对应分析标签标记 | DS-07 |
| 9 下拉缺统一样式 | 局部select样式不一致，未形成组件状态契约 | 统一来源／题型等简单select；大分类使用可搜索筛选组件，同一套高度、箭头、焦点和错误态 | DS-10 |

## 教师端审计

| ID／优先级 | 页面及依据 | 问题与风险 | 改造与验收 |
|---|---|---|---|
| T01 P1 | 图书馆；library-workspace.tsx的library-categories，截图2/9 | 分类与其他筛选分离；分类卡数量不受限 | 换集中部位下拉；20+分类不改变工具条高度；分类、来源、搜索组合结果正确；清空及返回保留规则一致 |
| T02 P1 | 题库编辑；question-bank.tsx persist/finish，lib/question-bank.mjs saveQuestion | 最新版本保存为草稿后practiceOpen=false，编辑动作可能影响原已确认题可用性 | 区分工作草稿与已确认可用版本；确认新版本前原版本可继续使用，已发试卷冻结不变；新增专门回归 |
| T03 P1 | 题库编辑；截图3、footer与顶部返回 | 两个退出入口语义冲突，预览开在底部却标签不变化，确认后停留易重复点击产新版本 | 采用规范中的按钮矩阵；未改动保存禁用或无新版本；预览只读且可关闭；确认成功定位题库条目 |
| T04 P2 | 题库选项；question-option | 每项上移／下移／删除并列挤占选项文本，长中文易变窄 | 行内保留正确项和输入，排序／删除用低权重工具；保留文字替代与键盘操作；长选项、窄屏不遮挡 |
| T05 P2 | AI出题；question-bank.tsx | 可点击按钮仅抛未接入错误，操作预期与能力不一致 | 服务不可用时展示统一不可用状态及简短原因；真实服务接入后补处理中、失败重试、草稿核对，不能假成功 |
| T06 P1 | 组卷；bank-paper-workspace.tsx | “新建／继续组卷”合并意图，默认勾选会替换当前选择；一页题库、配置、长题序缺阶段感 | 新建与继续草稿入口分明；页内组织选题→配置→发放检查；替换已有选择需提示；选片数量、总分持续可见 |
| T07 P2 | 发卷列表；bank-paper-workspace.tsx | 直接输出带T的时间，缺搜索与生命周期筛选；规模增长难查 | 日期本地化；采用列表模板及名称／状态筛选，显示提交／复核进度，不新增无依据仪表盘 |
| T08 P1 | 阅卷；grading-workspace.tsx，前轮浏览器 | 名单和答卷标题显示UUID而非姓名，教师难以识别人；整卷长表上下反复找操作 | 姓名为主、账号次要、编号仅细节；增加题目定位与未评分定位，保存位置明确；名单权限保持原逻辑 |
| T09 P1 | 阅卷；dirty事件、save与publish | 未保存保护存在，但没有持久化评分草稿；发布前未展示还有几份未复核 | 明确本次评分保存状态及未复核数；刷新恢复策略补齐；发布只使用已保存版，失败保留输入 |
| T10 P1 | 教学记录／能力总览；teacher-ability-module.tsx，截图4–7 | 双标题、重复录音区、无意义返回、业务动作夹杂筛选 | 根页只一个标题；录音与开展教学同属教学记录页头，能力总览不出现创建块；筛选条无创建动作 |
| T11 P1 | 分析详情；截图1/8 | 四维卡先于记录标题，完成卡与标签重复 | 改为“对象标题→标签→本次证据”，删除重复卡；异常有定位、解释、重试能力才展示 |
| T12 P1 | 能力总览；teacher-ability-module.tsx overview | 固定覆盖率、有效证据数量与示意雷达易被读成真实量化；数量与实际可点证据不一一对应 | 示例短标识收敛；不显示无法追溯的数量；聚合对象、时间、来源清楚；每个证据入口对应实际列表 |

## 学生端审计

| ID／优先级 | 页面及依据 | 问题与风险 | 改造与验收 |
|---|---|---|---|
| S01 P1 | 图书馆，共享library-workspace.tsx | 同T01，大分类卡会把学习内容推到下屏 | 师生共用筛选组件，只按权限改变内容；学生仍不可见教师参考 |
| S02 P1 | 日常练习；practice-workspace.tsx change/return | 保存失败提示存在，但返回练习可丢失尚未保存输入，缺与考试一致的离开保护 | 未保存时保留输入并阻止无提示离开；明确重试、复制与放弃；不可只显示一条错误后继续丢数据 |
| S03 P2 | 简答工具；practice-workspace.tsx format，exam-workspace.tsx | 练习所谓加粗插入Markdown标记，不是富文本；考试没有同等工具 | 定义一致的基础文本体验并复用；明确标记或实际格式，不误称富文本；练习与考试输入操作一致 |
| S04 P1 | 练习／考试；practice-columns、ImageReader | 窄屏已拆标签，但切题视野重置，临床/图片/作答上下文连续性不足 | 题级保存图片、视野和当前内容标签；回题／刷新恢复；无标定仍只显示相对缩放；不同题不能串用坐标 |
| S05 P2 | 练习结果；AI追问details | 结果后的追问被折叠且当前未接入；与未来右侧辅导定位需一致 | 仅提交后开放入口；宽屏右侧辅助，窄屏独立可关闭区域；未接入短状态，不出现可用发送假象 |
| S06 P2 | 考试中心；exam-workspace.tsx | 状态主要藏在按钮文案，所有未开始不可用项合并“尚未开放／已结束” | 独立状态标签区分未开放、进行中、已截止、待发布、已发布；按钮只表达下一步动作 |
| S07 P2 | 考试确认；exam-workspace.tsx | 页内alertdialog已替代原生确认，但还需焦点进入／返回和上下文关闭检查 | 取消后回按钮，确认时明确题目／空题数；返回列表不能残留旧确认；键盘不误提交另一试卷 |
| S08 P2 | 学习记录／错题；learning-workspace.tsx | 全部空态统一“暂无”，缺单题重置筛选；来源先于搜索，和图书馆不一致 | 搜索在前，来源／时间紧邻；区分无记录与筛选无结果并提供清空；日期倒置及时提示 |
| S09 P1 | 能力分析；learning-workspace.tsx ability | 五张“未评估”卡重复且面积大，真正作答证据被压后 | 无有效评估时一条紧凑说明＋五维列表，不画雷达；实际记录在前，条件充分才扩展维度卡 |
| S10 P2 | 学习记录详情；learning-workspace.tsx | 长卷一次展开全部内容，版本只写数字，不清楚是题目版或发布版 | 标明题目版本／成绩发布版本；题目导航与可折叠原答证据；不得展示未发布参考 |

## 跨端系统性问题

| ID | 依据／范围 | 处理 |
|---|---|---|
| C01 P1 | globals.css多处select、button规则；ui-button.tsx仅覆盖部分页面 | 建立共享组件契约及展示用例，旧教师页面逐个迁移；不是仅覆盖CSS以为行为已统一 |
| C02 P2 | teacher-record-table窄屏min-width:850px | 移动端横滚必须局限在表内，关键姓名与操作可找到；390px实测，当前仅源码判断风险 |
| C03 P2 | question-actions sticky，编辑页预览位于其后 | 检查软键盘／200%缩放遮挡；预览应有明确容器与关闭，不埋在底栏之后 |
| C04 P2 | teaching-platform及教师模块多重提示 | 环境提醒保留一处，示例只用短标签，风险按操作即时出现；禁止换成隐藏真实状态 |
| C05 P2 | README“当前状态”仍写旧阶段15项测试与未实现阅卷 | 文档索引与实际进度分离，旧阶段加历史标识；以后验收结论必须附时间、范围、证据，不沿用过期通过声明 |

## 整改顺序与完成条件

1. **结构批**：T10/T11/T12/S09及9条批注中的冗余清理。验收：业务入口唯一、标题先行、无重复卡、示例仍辨识。
2. **组件批**：T01/S01/C01，集中筛选及组件状态。验收：20+部位、长中文、键盘、390/768/1280/1440px可用。
3. **状态批**：T02/T03/T06/T09/S02/S07。验收：草稿不影响已发布内容、退出不丢输入、预览不写数据、确认不会重复创建。
4. **效率批**：姓名、日期、题目导航、版本文案和空态统一；补阅片连续性与基础文本工具。
5. **复验批**：师生完整流程、键盘／缩放／窄屏、浏览器刷新与保存失败；缺医学规则和真实API的能力单列依赖，不以设计验收冒充上线验收。

本轮交付是规范和审计，不包含上述整改代码。所有项保持“待整改／待实测”，没有把截图看起来整齐等同功能验收通过。
