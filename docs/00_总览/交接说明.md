# 交接说明 · path-edu-platform

更新时间：2026-09-11（接管回合）。写给**下一位没有聊天记录的开发者或 Agent**。
先读 [AGENTS.md](../../AGENTS.md) → [PROJECT_STATUS.md](./项目状态.md) → 本文 → [DECISIONS.md](./关键决策.md)。
前端改动前必读 [交互与UI原则.md](../05_设计系统/交互与UI原则.md)，并用 [师生端设计审计](../05_设计系统/师生端设计审计.md) 当设计债清单。

## 1. 浏览器验收：已可用（先看这条）

**Playwright 1.62.1 已经可用，不需要再安装、也不需要下载浏览器。**

- 它位于 `商汤医疗/node_modules/playwright`（注意：在**工作区上一级**），对本仓库直接 `import 'playwright'` 即可解析到。
- 脚本用 `channel:'chrome'` 调**系统 Chrome**（`/Applications/Google Chrome.app`），所以 Playwright 自带的浏览器二进制并不需要。
- `package.json` 里**没有**测试依赖，这是刻意的：Playwright 是环境预装件，不要把它写进项目依赖（AGENTS.md：不引入不必要的新依赖）。

> **更正**：2026-09-11 接管回合曾判断「本机未安装 Playwright」，**那是误判**——当时的 `find` 受沙箱限制，没能遍历到上一级 `node_modules`。`docs/03_实施与进度/账号测试与交付记录.md` 中「Playwright 使用桌面预装依赖」的说法是**正确的**。

跑法：
```sh
pnpm dev                                         # 终端 A：http://localhost:3002
node scripts/verify-annotation-tail-browser.mjs  # 终端 B（第五批验收，含 20+ 部位与确认入库定位）
NATIVE_ZOOM=200 node scripts/verify-annotation-tail-browser.mjs   # 200% 原生缩放
node scripts/verify-layout-browser.mjs           # 11 模块 × 1440/1280/768/390px
node scripts/verify-audit-editor-browser.mjs     # 键盘/焦点/草稿恢复
```

2026-09-11 实测：以上 4 项（含 200% 缩放）**全部 PASS**。

### 4 个脚本已过期，别被它们误导（K11）

`verify-navigation-browser` / `verify-demo-browser` / `verify-publishing-browser` / `verify-detail-browser` **必然超时失败**，因为选择器指向 2026-09-09 信息架构改造**之前**的旧界面（「病例资料」已改名「切片图书馆」、旧发布流程已换成题库组卷、根页已去掉返回按钮、演示横幅文案已改）。

**这不是回归。** 在按下述方式处理前，不要把它们的失败当成缺陷：
1. 按现界面重写（推荐，尤其是 `verify-detail-browser`，它覆盖**详情全流程**这项仍未验证的内容）；或
2. 明确标注弃用并从文档里移除引用。

### 仍未验证

`存储故障注入`（浏览器侧）、`详情全流程`（脚本已过期）。这两项**没有任何实测证据**，不要标成通过。

## 2. 本地运行

```sh
pnpm install --frozen-lockfile
pnpm test          # 52 项
pnpm typecheck
pnpm build
pnpm dev           # http://localhost:3002
```
演示版无需数据库。真实账号入口 `/account` 需要按 `.env.example` 准备 `.env.local` 再 `pnpm db:migrate` + `scripts/database.mjs bootstrap`。

## 3. 下一步任务（按优先级）

### P1 · 收尾第五批并把证据补齐

第五批设计整改（T07／S08／S10／C04）已在工作区完成，测试 52 项通过、构建通过，但**未提交、未推送、未在浏览器复验**。接手动作：

1. 想办法恢复浏览器验收能力（见 §1，需用户决定装不装测试依赖）。
2. 跑 `verify-annotation-tail-browser.mjs`，把结果写进 [审计文档](../05_设计系统/师生端设计审计.md) 第五批的「验证边界」列——**按 AGENTS.md：证据要在验证之后更新，不能因为编译通过就写通过**。
3. 确认后再决定提交与推送；推送／部署由用户主导，不要自动推。

### P2 · S05 练习结果 AI 追问区域（唯一还没动的设计审计代码项）

审计要求的验收标准（`师生端设计审计` S05 行）：**仅提交后开放入口；宽屏右侧辅助；窄屏独立可关闭区域；未接入短状态；不出现可用发送假象**。

现状：`app/practice-workspace.tsx` 里是答案卡内的一个折叠 `<details><summary>AI追问</summary>`，含禁用 textarea 与禁用「发送」。后两条（短状态、不伪造可用）**已满足**；缺的是**位置**。

改动前必须知道的三个约束（已核对源码，避免踩坑）：

1. `.practice-columns` 与 `.practice-show-*` 由**练习页和考试页共用**（`app/exam-workspace.tsx` 也在用）。所以任何改动必须挂**修饰类**（如 `.practice-columns.has-assist`），否则会连带改掉考试页。
2. 窄屏靠 `app/globals.css` 里 `:nth-child(1..3)` 的显隐规则实现。加第 4 个面板必须同时补 `:nth-child(4)` 的隐藏与显示规则，并注意选择器优先级：`.practice-columns.has-assist > :nth-child(4){display:none}` 比 `.practice-show-assist > :nth-child(4){display:block}` 更具体，显示规则要写得更具体或放在后面，否则面板永远不显示。
3. `app/use-answer-part.ts` 把标签白名单硬编码为 `['history','image','answer']`。若要给追问加标签，必须扩展白名单，**并且**在「恢复出 assist 标签但当前没有提交结果」时回退到 `answer`，否则窄屏可能整屏空白。

宽度测算（决定断点，别拍脑袋）：`.edu-layout` 是 `208px + 1fr`，工作区左右各 24px 内边距，所以内容宽 ≈ 视口 − 256px。
现有三列最小宽合计 `180+280+260`，加上追问列 `220`，再加 3×16 间隙 = `988px`。
- 1440 视口 → 内容 1184px，余量约 196px，可以四列。
- 1280 视口 → 内容 1024px，余量仅约 36px，勉强但不稳。
- 1200 视口 → 内容 944px，**不够，会横向溢出**，而「无整页横向溢出」是设计门禁的一条。

建议：四列只在更宽的断点启用（如 ≥1400px），**1399px 以下复用现有标签模式**——但那意味着要把标签回退区间从现在的 `≤1100px` 扩展到 `≤1399px`（仅对有追问面板的练习页生效）。改动不大，但涉及共用 CSS，**必须浏览器复验，不要在拿不到浏览器的情况下改完就宣布完成**。

### P3 · 剩余文档与技术债（原一致性项已修，余下未动）

- ~~进度表批次 6 状态~~、~~文件名版本与正文不一致~~ **已于 2026-09-11 修复**（K6／K7 已解决）。
- `app/globals.css` 的 `select[aria-invalid=true]` 是第五批新加的**无效样式**（全仓库没人设置 `aria-invalid`），`var(--danger)` 也未定义。按 AGENTS.md **只记录、不擅自删除**（K8）。
- `docs/99_归档/` 内 74 处失效链接**有意保留**，不要"修好"它们：归档是快照，改写等于篡改（K10／D-45）。

### P4 · 文档维护约定（新增，务必遵守）

文档已于 2026-09-11 统一收进 `docs/`，`docs/README.md` 是**唯一入口**（D-43／D-44）。

- 新文档放进对应分类目录，并在 `docs/README.md` 登记；不要再往仓库根或 `04_教学平台/` 放文档。
- 文件名用**用途名**，版本号只写在正文。
- 路径变更后跑一遍链接检查：仓库内所有活动文档的相对链接必须可解析（归档除外）。

## 4. 本回合做了什么（可复核）

- 实测基线：`pnpm test` 52 项通过、`pnpm typecheck` 通过、`pnpm build` 通过。
- 修掉第五批引入的一个**重复显示**缺陷：练习记录在详情里把版本显示了两遍（记录标题「题目版本 2」+ 逐题「题目版本 2」）。已用脚本实测确认复现，修复后确认消除；改动见 `lib/learning-records.mjs`（逐题 `questionVersion` 置空并注释原因）与 `tests/learning-records.test.mjs`（新增断言锁定该不变量）。这符合审计自身的 DS-07「状态只出现一次」。
- 补齐项目状态／交接说明／关键决策三份文档（现位于 `docs/00_总览/`）。
- **文档统一整理**：仓库外散落的 PRD／进度／依赖／测试／设计文档全部迁入 `docs/`，与代码同版本控制；按用途分七类并重命名；合并重复文档；历史快照归档；修复 85 处失效链接。详见 [项目状态](./项目状态.md) §5.2。
- **未做**：没有浏览器验收、没有提交、没有推送、没有部署、没有新增依赖、没有删任何既有代码（归档与合并不计为删除，原文可追溯）。

## 5. 给下一位的提醒

- **不要依赖聊天记录**。仓库里的代码、测试和这三份文档才是事实来源。
- 医学内容一律**不要伪造**：AI、录音、能力评分都是未接入状态，界面上的「示例」标注是有意保留的，不要当成冗余文案删掉。
- 演示数据只在当前浏览器 localStorage 里，`pnpm start`（生产模式）**不支持 PGlite**，要体验账号必须用 `pnpm dev`。
- 修改前先确认改的是演示层还是真实账号层（见 PROJECT_STATUS §3），两者证据标准不同。
- 保持独立项目边界：不要动旧科研平台。
